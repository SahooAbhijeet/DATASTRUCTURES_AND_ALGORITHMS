// This algorithm is used to find the minimum spanning tree .
// Minimum spanning tree is the spanning tree whose is sum is minimum.
// Spanning tree is the tree in which all nodes are reachable from each other.

#include<bits/stdc++.h>
using namespace std;

class Solution {
    public:
    int spanningTree(int V, vector<vector<int>> adj[]) {

    }
};